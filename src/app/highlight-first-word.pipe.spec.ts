import { HighlightFirstWordPipe } from './highlight-first-word.pipe';

describe('HighlightFirstWordPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightFirstWordPipe();
    expect(pipe).toBeTruthy();
  });
});
